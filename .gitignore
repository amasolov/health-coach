# Environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
*.egg-info/
dist/
build/
*.egg

# Garmin OAuth tokens (auth handled per-machine)
.garminconnect/
.garmin_tokens/

# iFit cached token and MITM captures
.ifit_token.json
.ifit_capture/
.ifit_login_debug.png

# IDE / editor config (contains machine-specific paths and secrets)
.vscode/
.cursor/
*.swp
*.swo
*~

# Chainlit session data (keep .chainlit/config.toml)
.chainlit/.langchain.db
.chainlit/translations/

# OS
.DS_Store
Thumbs.db

# Decrypted secrets (never commit)
secrets.yaml
secrets.dec.yaml

# Personal athlete data -- lives in /config/health-tracker/ at runtime, not in repo
config/athlete.yaml
config/equipment.yaml
config/zones.yaml
